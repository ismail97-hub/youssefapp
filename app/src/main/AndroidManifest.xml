<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.locationapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".insertionvente"> <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".Listedesvente"></activity>
        <activity android:name=".insertionvoiture">

        </activity>
        <activity android:name=".insertionclient" />
        <activity android:name=".manuprincipal" />
        <activity android:name=".listeVente" />
        <activity
            android:name=".Historique"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Charge"
            android:parentActivityName=".ventecharge" />
        <activity
            android:name=".vente"
            android:parentActivityName=".ventecharge" />
        <activity
            android:name=".ventecharge"
            android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>